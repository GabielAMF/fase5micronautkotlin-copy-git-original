micronaut:
  application:
    name: zup
datasources:
  default:
    url: jdbc:postgresql://localhost:5433/micronaut
    driverClassName: org.postgresql.Driver
    username: proposta
    password: SuperSenha
#    schema-generate: CREATE_DROP
#    dialect: H2
#jpa.default.properties.hibernate.hbm2ddl.auto: update
jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update